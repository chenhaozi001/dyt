# 把通话复盘助手免费部署成网站（保姆级图文步骤）

目标：让你拿到一个**网址（链接）**，在任何电脑/手机浏览器打开就能用，**不用装任何软件、不用打命令**。

整个过程**全程免费、不用信用卡**，分两步：
1. 领一个**免费的 AI Key**（让网站能真正分析，约 2 分钟）；
2. 把网站**一键部署**到 Streamlit 免费平台（约 5 分钟）。

> 关于「Cursor 会员」：Cursor 会员只能在 Cursor 软件里用，**不能**给这个外部网站调用 AI。
> 所以需要单独领一个免费 AI Key，下面第一步就是教你领。

---

## 第一步：领一个免费的 AI Key（Groq，不用信用卡）

1. 打开 https://console.groq.com/keys
2. 用 Google 账号或邮箱注册登录（免费）。
3. 点「Create API Key」，起个名字，点创建。
4. **把生成的一长串 Key 复制下来**，先存到记事本里（页面关了就看不到了）。

> 为什么用 Groq？它**完全免费、速度快、不用绑卡**，而且和通用接口兼容。
> 如果你公司有自己的 AI 接口，也可以用公司的，填法见第二步。

---

## 第二步：免费部署到 Streamlit Cloud

### 2.1 准备
- 你需要一个 **GitHub 账号**（这个项目代码就在 GitHub 上）。如果没有，去 https://github.com 免费注册。
- 确认这个项目的代码已经在你的 GitHub 仓库里（本项目分支已经准备好了）。

### 2.2 部署
1. 打开 https://share.streamlit.io
2. 点「**Sign in with GitHub**」，用你的 GitHub 账号登录授权。
3. 登录后点「**Create app**」→ 选「**Deploy a public app from GitHub**」。
4. 按下面填：
   - **Repository（仓库）**：选择你的项目仓库（`chenhaozi001/dyt`）。
   - **Branch（分支）**：选择本功能所在分支（`cursor/ai-sales-sharing-kit-591f`），或合并后选 `main`。
   - **Main file path（主文件）**：填 `app.py`
5. （重要）点开「**Advanced settings**」→ 在「**Secrets**」框里粘贴下面内容，把 Key 换成你第一步领的：

   ```toml
   LLM_API_KEY = "把你领到的 Groq Key 粘到这里"
   LLM_BASE_URL = "https://api.groq.com/openai/v1"
   LLM_MODEL = "llama-3.3-70b-versatile"
   ```

6. 点「**Deploy**」，等 1-3 分钟，构建完成后你会得到一个网址，类似：
   `https://你的名字-xxxx.streamlit.app`

7. 打开这个网址，上传脱敏后的录音转写文件，点「开始分析」——搞定！

> 如果第 5 步没填 Secrets 也能部署，网站会显示**示例结果**；
> 想真分析，进网站后在**左侧栏**手动粘贴 Key 即可（一样能用，只是每次要自己填）。

---

## 常见问题

**Q：网址别人也能打开，安全吗？**
A：Streamlit 免费版的网址是公开的。所以**务必只上传脱敏数据**（去掉真实姓名、电话、公司名）。
做分享演示完全够用。如果要更私密，可在 Streamlit 设置里把 app 设为私有（限指定邮箱访问）。

**Q：网站打开很慢 / 提示在“睡眠”？**
A：免费版长时间没人用会休眠，点一下「唤醒」等十几秒就好，正常现象。

**Q：我不想用 GitHub / 觉得麻烦？**
A：也可以不部署，直接在自己电脑上双击 `webapp/启动网站.command`(Mac) 或 `webapp/启动网站.bat`(Windows) 本地使用，见 README。

**Q：想换成谷歌 Gemini 的免费 Key？**
A：去 https://aistudio.google.com/apikey 领免费 Key，然后 Secrets 改成：
   ```toml
   LLM_API_KEY = "你的 Gemini Key"
   LLM_BASE_URL = "https://generativelanguage.googleapis.com/v1beta/openai/"
   LLM_MODEL = "gemini-2.0-flash"
   ```

---

## 我已经替你准备好的东西

- `app.py`：在线版网站程序（上传文件即出复盘）
- `requirements.txt`：已包含 streamlit，平台会自动安装
- `.streamlit/config.toml`：网站配色
- 你只需要按上面两步点鼠标即可，无需写任何代码。
